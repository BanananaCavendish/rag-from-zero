# ============ LangChain 1.x 系列 ============
# 注意:langchain / core / openai / text-splitters 升到 1.x;
#     langchain-community 保持 0.3.x(官方不升 1.0)。
langchain>=1.0,<2
langchain-core>=1.0,<2
langchain-openai>=1.0,<2
langchain-community>=0.3.15,<0.4
langchain-text-splitters>=1.0,<2

# ============ LLM / 向量 / 检索 ============
openai>=1.40              # langchain-openai 依赖;DeepSeek 走 OpenAI 兼容接口
faiss-cpu>=1.8.0          # 向量库(Chroma 在 Windows+Anaconda 段错误,见 README 坑#4)
sentence-transformers>=3.0
# 注意:torch 2.13+ 在 Anaconda Python 下有 DLL 加载失败问题(WinError 1114),
# 本项目锁定 2.6.0(已验证可用)。升级前先在 Windows 上跑通 import torch。
torch==2.6.0
rank-bm25>=0.2.2
jieba>=0.42.1             # 中文 BM25 预分词必需

# ============ 文档解析 ============
pypdf>=4.0                # PyPDFLoader 依赖
python-docx>=1.1          # 生成 docx 语料
docx2txt>=0.8             # Docx2txtLoader 依赖
beautifulsoup4>=4.12      # BSHTMLLoader 依赖
reportlab>=4.0            # 生成 PDF 语料(需注册中文字体)

# ============ 服务 ============
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # 文件上传必需
pydantic>=2.6

# ============ 评估 ============
# ragas>=0.2  # 可选:进阶替代手写评估 harness
